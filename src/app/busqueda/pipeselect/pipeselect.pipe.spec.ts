import { PipeselectPipe } from './pipeselect.pipe';

describe('PipeselectPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeselectPipe();
    expect(pipe).toBeTruthy();
  });
});
